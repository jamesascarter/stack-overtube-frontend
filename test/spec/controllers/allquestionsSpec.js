// 'use strict';

// describe('AllquestionsCtrl', function () {
//   var scope, ctrl, $httpBackend

//   beforeEach(module('stackOverTubeApp'));
//   beforeEach(inject(function(_$httpBackend_, $rootScope, $controller) {
//     $httpBackend = _$httpBackend_;
//     $httpBackend.expectGET('https://overtube-backend.herokuapp.com/allquestions').
//         respond([{name: 'Nexus S'}, {name: 'Motorola DROID'}, {name: 'dono'}]);

//     scope = $rootScope.$new();
//     ctrl = $controller('AllquestionsCtrl', {$scope: scope});
//   }));

//   it('should attach a list of questions to the scope', function () {
//     scope.getQuestions()
//     expect(scope.questions.length).toBe(3);
//   });
// });
